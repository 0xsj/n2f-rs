{
  "language": "n2f-rs",
  "module": "id",
  "result": {
    "chunk_id": "920bbf",
    "wall_time_seconds": 0.426110292,
    "exit_code": 101,
    "original_token_count": 620,
    "output": "   Compiling n2f-rs v0.1.0 (/Users/sj/Desktop/dev/builds/blueprints/n2f-rs)\nerror[E0432]: unresolved imports `n2f_rs::shared::id::Id`, `n2f_rs::shared::id::V7`, `n2f_rs::shared::id::Sequence`, `n2f_rs::shared::id::EntropyError`\n --> tests/id_spec.rs:2:90\n  |\n2 | use n2f_rs::shared::{clock::FakeClock,errors::{Classified,Failure,Kind,public_info},id::{Id,V7,Sequence,EntropyError}};\n  |                                                                                          ^^ ^^ ^^^^^^^^ ^^^^^^^^^^^^ no `EntropyError` in `shared::id`\n  |                                                                                          |  |  |\n  |                                                                                          |  |  no `Sequence` in `shared::id`\n  |                                                                                          |  no `V7` in `shared::id`\n  |                                                                                          no `Id` in `shared::id`\n\nerror[E0282]: type annotations needed for `Arc<_, _>`\n  --> tests/id_spec.rs:42:261\n   |\n42 | ...=(0..10).map(|_|{let g=Arc::clone(&g);thread::spawn(move||(0..100).map(|_|g.lock().unwrap().new_id().unwrap()).collect::<Vec<_>>())})....\n   |                         ^                                                      ---- type must be known at this point\n   |\nhelp: consider giving `g` an explicit type, where the type for type parameter `T` is specified\n   |\n42 |  let c=Arc::new(FakeClock::new(UNIX_EPOCH+Duration::from_millis(TICK)));let mut system=V7::new(||c.now());assert_eq!(system.new_id().unwrap().version(),7);let g=Arc::new(Mutex::new(V7::with_entropy(move||c.now(),zeros)));let workers:Vec<_>=(0..10).map(|_|{let g: Arc<T, A>=Arc::clone(&g);thread::spawn(move||(0..100).map(|_|g.lock().unwrap().new_id().unwrap()).collect::<Vec<_>>())}).collect();let mut seen=HashSet::new();for worker in workers{for v in worker.join().unwrap(){assert!(seen.insert(v));}}assert_eq!(seen.len(),1000);\n   |                                                                                                                                                                                                                                                                      +++++++++++\n\nSome errors have detailed explanations: E0282, E0432.\nFor more information about an error, try `rustc --explain E0282`.\nerror: could not compile `n2f-rs` (test \"id_spec\") due to 2 previous errors\n"
  }
}
