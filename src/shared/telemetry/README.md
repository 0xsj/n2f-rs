# Telemetry

Validated trace identity and the shared outcome vocabulary are SDK-free leaves.
The otel directory owns concrete SDK delivery. HTTP declares its own observation
capability; root supplies the adapter and owns configuration and shutdown.

Read [the contract](CONTRACT.md) and [native APIs](API.md).
