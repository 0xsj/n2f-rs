# HTTP

Safe public problem values, completion classification and first-valid lifecycle
are implemented. The axum directory owns native ingress/response behavior;
otel implements its observation capability. Root owns routes and process lifecycle.

Read [the contract](CONTRACT.md), [native APIs](API.md) and [completion shapes](SHAPES.md).
Run instructions and verification live in [the slice guide](../../../TELEMETRY_HTTP.md).
